homepage: "https://jumpdot.cl"
documentation: "https://jumpdot.cl/docs/gtm-template"
versions:
  # Each entry is one published version. "sha" must be the FULL 40-character SHA of a
  # commit in this repository whose template.tpl you want the gallery to serve, newest
  # first. After the first commit that contains template.tpl, replace the placeholder
  # below with that commit's SHA and commit this file again (the metadata commit itself
  # is never listed here).
  - sha: "9ec59816147981f85e3a32f51005c944975ec676"
    changeNotes: "Initial release."
